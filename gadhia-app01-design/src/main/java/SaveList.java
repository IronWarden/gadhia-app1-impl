/*
 *  UCF COP3330 Summer 2021 Application Assignment 1 Solution
 *  Copyright 2021 Rishi Gadhia
 */

import java.util.ArrayList;

public class SaveList {
    // save and load is a combo so they belong in the same class

    public void saveList(LocalEvent localEvent, LocalEvent user, String path, String name) {
        // iterate through the list of list to find the list the user wants to save
        // use try and catch, and Formatter to create a file that will save the list
        // the Formatter path will be specified by the user in a string
        // the files name will be specified by the user in a string
    }

    public ArrayList<String> loadList(LocalEvent localEvent, LocalEvent user, String path, String name ) {
        // user will specify where the list is stored with a path string and the name of file
        // use reader to open the file and read the contents
        // to load a single list we take the data read and store it into a list of strings
        // return the list

        return null;
    }


}
